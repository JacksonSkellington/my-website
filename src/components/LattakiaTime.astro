---
const now = new Date();
const formatter = new Intl.DateTimeFormat('en-US', {
  timeZone: 'Asia/Damascus', // close enough i guess
  hour: '2-digit',
  minute: '2-digit',
  hour12: true,
});
const lattakiaTime = formatter.format(now);
---
<div>
  <h5>Syria, Lattakia<br>{lattakiaTime}</h5>
</div>
